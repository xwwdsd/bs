<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cs2trade.mapper.MessageMapper">
    <resultMap id="MessageMap" type="com.cs2trade.entity.Message">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="type" column="type"/>
        <result property="subType" column="sub_type"/>
        <result property="status" column="status"/>
        <result property="relatedOrderNo" column="related_order_no"/>
        <result property="relatedItemId" column="related_item_id"/>
        <result property="relatedSellOrderId" column="related_sell_order_id"/>
        <result property="itemName" column="item_name"/>
        <result property="itemIconUrl" column="item_icon_url"/>
        <result property="bargainPrice" column="bargain_price"/>
        <result property="senderId" column="sender_id"/>
        <result property="senderName" column="sender_name"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="selectById" resultMap="MessageMap">
        SELECT m.*, i.icon_url AS item_icon_url
        FROM sys_message m
        LEFT JOIN item i ON i.id = m.related_item_id
        WHERE m.id = #{id}
    </select>

    <select id="getMessages" resultMap="MessageMap">
        SELECT m.*, i.icon_url AS item_icon_url
        FROM sys_message m
        LEFT JOIN item i ON i.id = m.related_item_id
        WHERE m.user_id = #{userId}
        <if test="type != null">
            AND m.type = #{type}
        </if>
        ORDER BY m.created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <sql id="Admin_Filter">
        <where>
            <if test="userId != null">
                AND m.user_id = #{userId}
            </if>
            <if test="type != null">
                AND m.type = #{type}
            </if>
            <if test="status != null">
                AND m.status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (
                    m.title LIKE CONCAT('%', #{keyword}, '%')
                    OR m.content LIKE CONCAT('%', #{keyword}, '%')
                    OR m.related_order_no LIKE CONCAT('%', #{keyword}, '%')
                    OR m.item_name LIKE CONCAT('%', #{keyword}, '%')
                    OR m.sender_name LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
        </where>
    </sql>

    <select id="selectAllForAdmin" resultMap="MessageMap">
        SELECT m.*, i.icon_url AS item_icon_url
        FROM sys_message m
        LEFT JOIN item i ON i.id = m.related_item_id
        <include refid="Admin_Filter"/>
        ORDER BY m.created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countAllForAdmin" resultType="long">
        SELECT COUNT(*)
        FROM sys_message m
        <include refid="Admin_Filter"/>
    </select>

    <select id="countMessages" resultType="int">
        SELECT COUNT(*) FROM sys_message 
        WHERE user_id = #{userId} 
        <if test="type != null">
            AND type = #{type}
        </if>
    </select>

    <insert id="createMessage" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_message (
            user_id, title, content, type, sub_type, status,
            related_order_no, related_item_id, related_sell_order_id, item_name, bargain_price,
            sender_id, sender_name, created_at
        ) VALUES (
            #{userId}, #{title}, #{content}, #{type}, #{subType}, 0,
            #{relatedOrderNo}, #{relatedItemId}, #{relatedSellOrderId}, #{itemName}, #{bargainPrice},
            #{senderId}, #{senderName}, NOW()
        )
    </insert>

    <select id="countPendingBargains" resultType="int">
        SELECT COUNT(*)
        FROM sys_message
        WHERE type = 3
          AND sub_type = 1
          AND sender_id = #{senderId}
          AND related_sell_order_id = #{relatedSellOrderId}
    </select>

    <update id="updateBargainStatus">
        UPDATE sys_message
        SET sub_type = #{subType},
            status = #{status},
            related_order_no = #{relatedOrderNo},
            content = #{content},
            title = #{title}
        WHERE id = #{id}
    </update>

    <update id="markAsRead">
        UPDATE sys_message SET status = 1 WHERE id = #{id}
    </update>

    <update id="batchMarkAsRead">
        UPDATE sys_message SET status = 1 
        WHERE id IN 
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="markAllRead">
        UPDATE sys_message SET status = 1 
        WHERE user_id = #{userId} AND status = 0
        <if test="type != null">
            AND type = #{type}
        </if>
    </update>

    <delete id="batchDelete">
        DELETE FROM sys_message 
        WHERE id IN 
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*) FROM sys_message 
        WHERE user_id = #{userId} AND status = 0
        <if test="type != null">
            AND type = #{type}
        </if>
    </select>
</mapper>
